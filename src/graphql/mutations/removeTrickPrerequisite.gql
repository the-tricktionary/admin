mutation RemoveTrickPrerequisite ($trickId: ID!, $prerequisiteId: ID!) {
  removeTrickPrerequisite (trickId: $trickId, prerequisiteId: $prerequisiteId) {
    id
    prerequisites {
      id
      slug
      en: localisation {
        id
        name
      }
    }
  }
}
